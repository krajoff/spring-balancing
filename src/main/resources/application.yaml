server:
  port: 8181

spring:
  datasource:
    url: jdbc:postgresql://localhost:15432/balancing
    username: root
    password: passwords
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

