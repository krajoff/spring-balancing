server:
  port: 8080

spring:
#  datasource:
#    url: jdbc:postgresql://127.0.0.1:5432/balancing
#    username: root
#    password: passwords
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

